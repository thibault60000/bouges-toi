import styled from "styled-components";

import { Exit } from "styled-icons/boxicons-regular/Exit";
import { EditAlt } from "styled-icons/boxicons-solid/EditAlt";
import { DeleteForever } from "styled-icons/material/DeleteForever";
import { Enter } from "styled-icons/icomoon/Enter";
import { Cross } from "styled-icons/icomoon/Cross";

const ExitIcon = styled(Exit)`
  height: 1.8rem;
  vertical-align: middle;
  margin-right: 0.5rem;
`;

const CrossIcon = styled(Cross)`
  height: 1.4rem;
  vertical-align: middle;
`;

const EditAltIcon = styled(EditAlt)`
  height: 1.8rem;
  vertical-align: middle;
  margin-right: 0.5rem;
`;

const DeleteForeverIcon = styled(DeleteForever)`
  height: 1.8rem;
  vertical-align: middle;
  margin-right: 0.5rem;
`;

const EnterIcon = styled(Enter)`
  height: 1.8rem;
  vertical-align: middle;
  margin-right: 0.5rem;
`;

export { ExitIcon, EditAltIcon, DeleteForeverIcon, EnterIcon, CrossIcon };